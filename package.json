{
  "name": "create-tailwindcss-app",
  "version": "1.0.0",
  "description": "to help developers just install this and start code ,no need to setup",
  "main": "tailwind.config.js",
  "dependencies": {
    
  },
  "devDependencies": {
    "tailwindcss": "^3.3.5"

  },
  "scripts": {
    "start":"tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jahangeer",
  "license": "ISC"
}
